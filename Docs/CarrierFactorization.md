## Lane 3: Algebraic Carrier Candidates\n+\n+The physics closure construction already implicitly sits over a structured carrier. Lane 3 needs to surface the strongest plausible factorization surfaces that the current Agda code could pass through. Candidate carriers (and factorization hypotheses) are:\n+\n+1. **Finite abelian quotient**: the shift carrier is `Vec Trit (m + k)`, so one hypothesis is that the relevant structure factors through a finite abelian group `Tⁿ` (trit vectors) modulo an MDL-defined relation. The repo gap is that no abstract quotient record currently isolates the `coarse` kernel or the `MDL` kernel.\n+\n+2. **Chinese Remainder/Theta decomposition**: the canonical prime embedding `shiftPrimeEmbedding` already maps into a 15-coordinate factor vector (`GL.v15`). Factorization meaning: the carrier factors through a CRT-lattice on the 15 Monster primes, with compatibility defined via the Hecke signature. The gap is the lack of a general CRT record (an injective mapping plus per-prime compatibility constraints) in the code.\n+\n+3. **Modular level / Hecke-like family**: the Hecke pipeline is already parametrized by `SSP`, so we can hypothesize a Hecke family record with `T_p` operators and `compat p` booleans. The current repo already builds `shiftHeckeAction` and `shiftCompat` explicitly, but there’s no abstract `HeckeFamily` record with a lattice/level hypothesis that other carriers could instantiate. Filling this gap would allow alternative carrier realizations (for example, modular forms) to use the same Hecke survey.\n+\n+4. **Root lattice / Weyl action**: The shift geometry uses signed permutations (`SP.SignedPerm4`), so another hypothesis is that the carrier factors through a Weyl-group-invariant lattice (`Vec ℤ 4` plus `SP` action). The gap is that this symmetry is currently applied concretely; there is no abstraction capturing “carrier = vector lattice + Weyl action + hyperbolic cone,” so alternative root systems cannot plug in directly.\n+\n+5. **Ultrametric eigen-factorization**: the eigen/motif pipeline already splits states into Earth/Spoke/Hub counts. Hypothesis: there exists an ultrametric factorization where the carrier splits into a tree of finite factors. The repo gap is the absence of a general `UltrametricFactor` record that states how `shiftDeltaEigenProfile` or `shiftMotifOf` factors through such a tree.\n+\n+In summary, the algebraic carrier candidates are: finite abelian quotient, CRT-like factor vector, modular/Hecke family, Weyl-invariant lattice, and ultrametric factor tree. Each candidate points to a missing abstract record (quotient kernel, CRT factorization, Hecke family record, root-lattice bundle, ultrametric factorization record). Closing Lane 3 requires defining at least one such file in `Docs/` and then providing the relevant Agda abstraction in a future lane.\n*** End Patch***
