# Plan 06-03: Assumption-Pack Burn-Down (Orbit Fingerprint + Dimension Bound)

## Goal
Reduce or eliminate postulates in the orbit fingerprint and dimension bound assumption modules by replacing them with computed/certified data and explicit bridges from existing computed profiles.

## Scope
- `DASHI/Physics/OrbitFingerprintAssumptionsPostulates.agda`
- `DASHI/Physics/DimensionBoundAssumptionsPostulates.agda`
- Related computed profile modules:
  - `DASHI/Physics/OrbitProfileComputed.agda`
  - `DASHI/Physics/OrbitProfileComputedTailPerm.agda`
  - `DASHI/Physics/ShellOrbitProfileGenerator.agda`
- Any small glue modules needed to connect computed profiles to the assumption packs.

## Constraints
- Keep closure spine postulate-free.
- Avoid breaking exports used by `SignatureUniquenessAssumed` and closure harnesses.
- Prefer concrete computed profiles over new postulates.
- Any remaining assumptions must be explicitly isolated behind a single gate module.

## Deliverables
- Postulates replaced by concrete values or new record fields with explicit evidence.
- If full discharge is not possible, reduce to one minimal “assumption gate” with documented inputs.
- Updated assumption-pack modules with clear provenance.

## Success Criteria
- `DASHI/Everything.agda` still typechecks.
- At least one of the assumption packs is fully discharged or materially reduced.
- Remaining assumptions (if any) are localized to a single file with explicit input evidence.

## Risks / Open Questions
- Orbit profile computation is finite and expensive; must remain total in Agda.
- Tail permutation orbit profiles may remain too large; may need to keep as assumptions for now.

## Next Actions
1) Inspect the two assumption modules for exact postulates.
2) Identify existing computed profile data that can replace those postulates.
3) Implement bridging lemmas to connect computed profiles to the assumption statements.
4) Rerun `agda -i . -i /usr/share/agda/lib/stdlib DASHI/Everything.agda`.
