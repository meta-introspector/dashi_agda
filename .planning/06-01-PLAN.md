# Plan 06-01: LCP Fixed-Point Wiring + P-Strict Witness

## Goal
Make the LCP fixed-point pipeline usable by the real operator stack and close the last “physics seam” by providing a guard-based `P-strict` witness.

## Scope
- Wire LCP Banach fixed point to the operator stack via `FixedPointFromTContract` / `ClosureKitLCP`.
- Provide a concrete, guard-based strictness lemma for `P` (tail collapse) in LCP depth.
- Decide and encode the guard predicate (first-difference or NotSnap) and its reachability/preservation under `T`.

## Constraints
- Keep existing exports intact.
- No new postulates in the concrete shift stack.
- Prefer guard-based strictness if global strictness is false.

## Deliverables
- Concrete `SeverityPolicy` for shift carrier: `codeᵣ` + thresholds.
- Concrete `Restoreᵣ` for broken states (Maass restoration hook).
- `P-strict-on` under Guard for `Pᵣ` in LCP language.
- `TContractiveDepthGuarded` instantiated with real C/P/R + Guard.
- Closure kit wiring demonstrating fixed point + uniqueness for the guarded LCP instance.

## Success Criteria
- `SeverityGuardShiftWiring` constructed from concrete `SeverityPolicy` and restore map.
- Guarded `P-strict-on` proved for `Pᵣ`.
- `TContractiveDepthGuarded` compiles with real C/P/R and Guard.
- No new postulates introduced in the shift stack.

## Status
In progress
